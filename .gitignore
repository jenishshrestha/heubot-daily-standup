# clasp credentials — script ID + auth token, never commit
.clasp.json
.clasprc.json

# Service account JSON keys — bot identity credential, never commit
heubot-*.json
*-service-account*.json

# bun (commit bun.lock for reproducible installs; only ignore node_modules)
node_modules/

# editor / OS noise
.DS_Store
.vscode/
.idea/

# logs
*.log
